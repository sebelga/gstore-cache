'use strict';

const chai = require('chai');

const { expect } = chai;

describe('gstore-cache', () => {
    describe('init()', () => {
        it('should work', () => {
            expect(true).equal(true);
        });
    });
});
